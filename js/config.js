// Central frontend config for Habit Tracker
// Edit these values after deploying functions and setting APP_URL
window.APP_URL = window.APP_URL || 'https://your-app.example';
// FUNCTIONS_BASE_URL should point to your Supabase Functions base URL, e.g.:
// https://<region>-<project>.functions.supabase.co
window.FUNCTIONS_BASE_URL = window.FUNCTIONS_BASE_URL || '';

// Optional: set a short flag for enabling email links to call functions directly
window.USE_FUNCTION_LINKS = window.USE_FUNCTION_LINKS || false;
